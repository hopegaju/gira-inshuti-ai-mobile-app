name: gira_inshuti
description: A Flutter application for community support and counseling.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2       
  cupertino_icons: ^1.0.6
  google_sign_in: ^6.2.1 
  firebase_auth: ^4.17.4 
  firebase_core: ^2.27.1
  cloud_firestore: ^4.16.0 
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true